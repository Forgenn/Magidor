<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.magidor" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_magi"
        android:label="Magidor"
        android:roundIcon="@mipmap/ic_launcher_magi_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kotlin" >
        <activity android:name="com.example.magidor.activities.GamePopUp" >
        </activity>
        <activity
            android:name="com.example.magidor.activities.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>